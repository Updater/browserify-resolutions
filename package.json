{
  "name": "browserify-resolutions",
  "version": "1.0.3",
  "description": "A Browserify plugin that allows more explicit control of module deduping. It purges duplicate modules from the output bundle and prevents modules from loading several times.",
  "main": "index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Updater/browserify-resolutions.git"
  },
  "keywords": [
    "browserify",
    "browserify-plugin",
    "resolutions",
    "dedupe",
    "duplication"
  ],
  "scripts": {
    "test": "mocha"
  },
  "dependencies": {
    "lodash": "^3.7.0",
    "through2": "^0.6.5"
  },
  "devDependencies": {
    "browserify": "^10.0.0",
    "chai": "^2.2.0",
    "mocha": "^2.2.4",
    "watchify": "^3.2.2"
  }
}
